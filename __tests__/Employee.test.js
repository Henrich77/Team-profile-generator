// require Employee
const Employee = require('./lib/Employee')


// write a test to see if you can instantiate an Employee instance
test( 'can instantiate Employee instance', () =>{
    const employee = new Employee()


    // variable for the new Employee
    // expect the type of that variable to be an object
});

// write a test to see if you can set the name of the constructor argument

test ( 'can set the name via constructor arguments', () => {

    // variable for the name 
    //variable for a new employee with the name passed in as an argument
    // expect the variable.name to be the variable that you can set up for the name
})

// same as above for id 


// same as above for email

// write a test to see if your getter method is working
test (' can get name via getName', () => {
    //variable for the test value
    //variable for the new employee with the test value passed in 
    // expect that when you call that method - to be the test value variable
}) 


// same for getting the id
// same for getting the email 
// same for getting the role